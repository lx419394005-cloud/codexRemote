{
  "name": "codex-bridge",
  "version": "1.0.0",
  "description": "A secure bridge to access Codex CLI remotely via JSON-RPC over WebSocket",
  "main": "bridge.js",
  "scripts": {
    "dev": "NODE_ENV=development node bridge.js",
    "build": "next build",
    "start": "NODE_ENV=production node bridge.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "next": "^16.2.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "ws": "^8.16.0"
  }
}
